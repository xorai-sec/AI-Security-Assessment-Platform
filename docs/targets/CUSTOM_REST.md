# Custom REST Targets

Custom REST targets use safe JSON field paths instead of executable mappings. Configure request template, prompt field, optional session/user-role fields, response text path and optional telemetry path.

