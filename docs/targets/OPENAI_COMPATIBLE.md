# OpenAI-Compatible Targets

Configure `base_url`, `chat_path`, `models_path`, `model_name`, headers and optional API key. The adapter sends real chat-completions requests and parses `choices.0.message.content`.

