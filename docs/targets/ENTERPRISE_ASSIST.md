# EnterpriseAssist Target

EnterpriseAssist remains the controlled laboratory fixture and regression target. It exposes native `/chat`, OpenAI-compatible `/v1/chat/completions` and Custom REST `/custom-chat` fixture interfaces.

