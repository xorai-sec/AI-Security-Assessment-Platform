# Target Adapters

Target adapters implement configuration validation, health checks, capability discovery, message sending, sessions, telemetry lookup, authentication testing and sanitization.

Implemented adapters:

- EnterpriseAssist
- OpenAI-compatible
- vLLM
- Ollama
- Custom REST

Generic RAG and agent targets use Custom REST with telemetry-dependent capabilities.

