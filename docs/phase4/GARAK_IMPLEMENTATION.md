# garak Implementation

The garak worker installs `garak==0.13.0`, validates CLI probe listing and executes controlled proxy-routed prompts. Full native garak generator integration is still a release blocker.

