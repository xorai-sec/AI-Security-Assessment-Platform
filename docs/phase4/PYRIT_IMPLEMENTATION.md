# PyRIT Implementation

The PyRIT worker installs the official `pyrit` package, reports runtime version and executes multi-turn objective prompts through the target proxy. Full PyRIT PromptTarget/scorer integration is still a release blocker.

