# Historical DeepTeam Implementation (Superseded)

> This document is retained for audit history only. DeepTeam has been removed
> from the active assessment stack.

The DeepTeam worker installs the official `deepteam` package, reports runtime version and executes proxy-routed vulnerability prompts. Full DeepTeam API callback integration is still a release blocker.
