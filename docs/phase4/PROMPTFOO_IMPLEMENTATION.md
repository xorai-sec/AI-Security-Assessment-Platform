# Promptfoo Implementation

The Promptfoo worker installs the official npm package, reports CLI version and executes assertion-style proxy-routed checks. Full Promptfoo provider/config execution is still a release blocker.

