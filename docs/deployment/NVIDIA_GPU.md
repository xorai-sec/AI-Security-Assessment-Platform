# NVIDIA GPU Deployment

Use `docker-compose.gpu.yml` for optional Ollama/vLLM model serving on NVIDIA systems. This repository was developed on an AMD/CPU-first environment, so GPU validation must be performed on compatible hardware.

