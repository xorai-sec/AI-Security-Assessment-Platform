services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aisec
      POSTGRES_PASSWORD: aisec
      POSTGRES_DB: aisec
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aisec"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  enterprise-assist:
    build:
      context: .
      dockerfile: apps/enterprise-assist/Dockerfile
    ports:
      - "8090:8090"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8090/health')"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    environment:
      ENTERPRISE_ASSIST_LOCAL_URL: http://enterprise-assist:8090
      AISEC_ALLOW_LOCAL_TARGETS: "true"
      AISEC_ALLOWED_LOCAL_HOSTS: 127.0.0.1,localhost,enterprise-assist,ollama,vllm
      TARGET_PROXY_SECRET: ${TARGET_PROXY_SECRET:-development-target-proxy-secret}
      FRAMEWORK_NATIVE_ENABLED: "true"
      FRAMEWORK_GARAK_ENABLED: "true"
      FRAMEWORK_PYRIT_ENABLED: "true"
      FRAMEWORK_PROMPTFOO_ENABLED: "true"
      FRAMEWORK_DEEPTEAM_ENABLED: "true"
      NATIVE_WORKER_URL: http://native-worker:8091
      GARAK_WORKER_URL: http://garak-worker:8092
      PYRIT_WORKER_URL: http://pyrit-worker:8093
      PROMPTFOO_WORKER_URL: http://promptfoo-worker:8095
      DEEPTEAM_WORKER_URL: http://deepteam-worker:8094
      API_INTERNAL_BASE_URL: http://api:8080
      DATABASE_URL: postgresql+psycopg://aisec:aisec@postgres:5432/aisec
      REDIS_URL: redis://redis:6379/0
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    depends_on:
      enterprise-assist:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health')"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "5173:80"
    depends_on:
      - api
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
